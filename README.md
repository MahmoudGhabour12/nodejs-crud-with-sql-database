# Sample Nodejs crud api

This is very sample crud api with nodejs and sql server using sequelize

## how to run server

- First use `yarn` or `npm i` to install require packages.

- Second go to 'config file' and add your sql database config.

- Third use `yarn start` or `nodemon server.js` to run server.
